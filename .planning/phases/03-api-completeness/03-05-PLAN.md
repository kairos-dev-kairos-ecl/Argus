---
phase: 03-api-completeness
plan: 05
type: execute
wave: 3
depends_on: ["03-02", "03-03", "03-04"]
files_modified:
  - internal/ingest/handler_response_test.go
  - internal/ingest/handler_apps_test.go
autonomous: true
requirements: [REQ-P3-08]

must_haves:
  truths:
    - "Tests verify layer status returns signal_count_5min field"
    - "Tests verify trace returns spans[] not signals[]"
    - "Tests verify query returns rows as objects not arrays"
    - "go test ./internal/ingest/... -run TestRulesNilPool -count=1 exits 0 (rules handlers return 503 with nil pg pool, proving DB-backed code path)"
    - "Tests verify apps create returns api_key field"
  artifacts:
    - path: "internal/ingest/handler_response_test.go"
      provides: "Response shape tests for layer status, trace, query, rules"
      min_lines: 50
    - path: "internal/ingest/handler_apps_test.go"
      provides: "Apps CRUD handler tests"
      min_lines: 30
  key_links:
    - from: "internal/ingest/handler_response_test.go"
      to: "internal/ingest/receiver_query.go"
      via: "httptest against handler methods"
      pattern: "httptest.NewRecorder"
---

<objective>
Write integration tests for all fixed and new endpoints using httptest.

Purpose: Verify response shapes match frontend expectations and prevent regression.
Output: Two test files covering response shapes and apps CRUD.
</objective>

<execution_context>
@C:/Users/Drupad/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Drupad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-api-completeness/03-02-SUMMARY.md
@.planning/phases/03-api-completeness/03-03-SUMMARY.md
@internal/ingest/receiver_query.go
@internal/ingest/handler_apps.go
@web/src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Response shape tests for layer status, trace, and query endpoints</name>
  <files>internal/ingest/handler_response_test.go</files>
  <read_first>
    - internal/ingest/receiver_query.go (LayerStatus, TraceResponse, QueryResultResponse structs)
    - internal/ingest/handler_rules.go (ServeListRules)
    - internal/ingest/handler_stubs.go (handleListRules with DB-backed implementation)
    - web/src/types/index.ts (expected field names)
  </read_first>
  <action>
Create `internal/ingest/handler_response_test.go` with these tests:

1. **TestLayerStatusResponseShape** -- Create a QueryHandler with nil ClickHouse (degraded mode). Call handleGetLayerStatus via httptest. Parse response JSON. Assert:
   - Response has `layers` array with 10 entries
   - First entry has `"layer": "L1_HARDWARE"` (string, not int)
   - First entry has `"status": "gray"` (not "unknown" or "idle")
   - First entry has `"signal_count_5min"` key (not `"signal_count"`)
   - First entry has `"last_signal_time"` key (not `"last_seen_at"`)
   - No `"name"` key present

2. **TestTraceResponseShape** -- This is harder without ClickHouse. Test the TraceResponse struct serialization directly:
   - Create a TraceResponse with sample SpanView data
   - Marshal to JSON
   - Assert JSON contains `"spans"` key (not `"signals"`)
   - Assert JSON contains `"detections"` key
   - Assert JSON contains `"duration_ms"` key
   - Assert SpanView has `"start_time"`, `"parent_signal_id"`, `"status"`, `"message"` keys

3. **TestQueryResponseShape** -- Test QueryResultResponse struct serialization:
   - Create a QueryResultResponse with sample row objects
   - Marshal to JSON
   - Assert JSON contains `"execution_time_ms"` key
   - Assert `"rows"` value is array of objects (not array of arrays)
   - Assert JSON contains `"total"` key
   - Assert no `"columns"` or `"row_count"` keys

4. **TestRulesNilPool** -- Create QueryHandler with nil pg pool (and a no-op zap logger). This test exercises BOTH GET and POST rule paths to prove the DB-backed code path is wired through (not the old StatusNotImplemented stub). Steps:
   - GET path: Call `handleListRules` via `httptest.NewRecorder` + `httptest.NewRequest("GET", "/api/v1/rules", nil)`. Assert:
     - `rec.Code == http.StatusServiceUnavailable` (503)
     - Response body contains `"database unavailable"` JSON error
   - POST path: Call `handleCreateRule` via `httptest.NewRecorder` + `httptest.NewRequest("POST", "/api/v1/rules", strings.NewReader("{}"))`. Assert:
     - `rec.Code == http.StatusServiceUnavailable` (503)
     - Response body contains `"database unavailable"` JSON error
     - Confirms the nil-pool guard runs BEFORE any JSON parsing, proving DB-backed wiring.
   This is a unit test (no DB needed) that proves the rule handlers are no longer 501 stubs and now require the PostgreSQL pool.

Use `testing` + `testify/assert`. Use `httptest.NewRecorder` + `httptest.NewRequest`.
  </action>
  <verify>
    <automated>cd C:/Users/Drupad/ArgusXDR && go test ./internal/ingest/... -run "TestLayerStatusResponseShape|TestTraceResponseShape|TestQueryResponseShape|TestRulesNilPool" -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/ingest/... -run TestLayerStatusResponseShape -count=1` exits 0
    - `go test ./internal/ingest/... -run TestTraceResponseShape -count=1` exits 0
    - `go test ./internal/ingest/... -run TestQueryResponseShape -count=1` exits 0
    - `go test ./internal/ingest/... -run TestRulesNilPool -count=1` exits 0
  </acceptance_criteria>
  <done>All four response shape tests pass, verifying frontend-compatible JSON field names and that rules handlers are no longer 501 stubs (TestRulesNilPool proves DB-backed code path on both GET and POST).</done>
</task>

<task type="auto">
  <name>Task 2: Apps handler unit tests (nil-pool degradation)</name>
  <files>internal/ingest/handler_apps_test.go</files>
  <read_first>
    - internal/ingest/handler_apps.go (handler implementations)
    - internal/ingest/receiver_query.go (QueryHandler struct, NewQueryHandler)
  </read_first>
  <action>
Create `internal/ingest/handler_apps_test.go` with these unit tests (NO database required — all use nil pool to exercise the graceful-degradation path):

1. **TestListAppsNilPool** -- Construct a `QueryHandler{}` with `pool == nil` (and a no-op zap logger). Call `handleListApps` via `httptest.NewRecorder` + `httptest.NewRequest("GET", "/api/v1/apps", nil)`. Assert:
   - `rec.Code == http.StatusServiceUnavailable` (503)
   - Response body contains `"storage unavailable"`

2. **TestCreateAppNilPool** -- Construct a `QueryHandler{}` with `pool == nil`. Call `handleCreateApp` via httptest with body `{"name":"test-app","description":"x"}` (POST `/api/v1/apps`). Assert:
   - `rec.Code == http.StatusServiceUnavailable` (503)
   - Response body contains `"storage unavailable"`
   - This confirms the nil-pool guard runs BEFORE any JSON parsing or validation, proving the handler is not a stub and gracefully degrades when PostgreSQL is unavailable.

3. **TestGetAppKeyNilPool** -- Same pattern: nil pool + `handleGetAppKey` → assert 503 + `"storage unavailable"`.

4. **TestRotateAppKeyNilPool** -- Same pattern: nil pool + `handleRotateAppKey` → assert 503 + `"storage unavailable"`.

Do NOT include `TestCreateAppValidation` — empty-name validation requires the pool guard to be bypassed (i.e. real DB) and would belong in an integration test. Any test that needs a live PostgreSQL connection MUST be in a separate file with `//go:build integration` build tag at the top, and is out of scope for this task.

Keep tests simple. The response shape tests in Task 1 plus these four nil-pool degradation tests are the gate.
  </action>
  <verify>
    <automated>cd C:/Users/Drupad/ArgusXDR && go test ./internal/ingest/... -run "TestListAppsNilPool|TestCreateAppNilPool|TestGetAppKeyNilPool|TestRotateAppKeyNilPool" -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/ingest/... -run TestListAppsNilPool -count=1` exits 0
    - `go test ./internal/ingest/... -run TestCreateAppNilPool -count=1` exits 0
    - `go test ./internal/ingest/... -run TestGetAppKeyNilPool -count=1` exits 0
    - `go test ./internal/ingest/... -run TestRotateAppKeyNilPool -count=1` exits 0
    - `grep -c "TestCreateAppValidation" internal/ingest/handler_apps_test.go` returns 0 (removed — no concealed dual-design TODO)
    - File `internal/ingest/handler_apps_test.go` exists
  </acceptance_criteria>
  <done>Apps handler tests verify graceful 503 degradation when PostgreSQL pool is nil, on all four endpoints. No ambiguous dual-design tests; integration-only tests excluded.</done>
</task>

</tasks>

<verification>
- `go test ./internal/ingest/... -count=1` passes (all tests including new ones)
- Response shape tests confirm frontend-compatible JSON
</verification>

<success_criteria>
- All response shape tests pass
- Apps degradation tests pass
- No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/03-api-completeness/03-05-SUMMARY.md`
</output>
