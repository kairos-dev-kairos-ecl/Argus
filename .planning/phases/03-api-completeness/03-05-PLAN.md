---
phase: 03-api-completeness
plan: 05
type: execute
wave: 3
depends_on: ["03-02", "03-03", "03-04"]
files_modified:
  - internal/ingest/handler_response_test.go
  - internal/ingest/handler_apps_test.go
autonomous: true
requirements: [REQ-P3-08]

must_haves:
  truths:
    - "Tests verify layer status returns signal_count_5min field"
    - "Tests verify trace returns spans[] not signals[]"
    - "Tests verify query returns rows as objects not arrays"
    - "Tests verify rules endpoints return non-501"
    - "Tests verify apps create returns api_key field"
  artifacts:
    - path: "internal/ingest/handler_response_test.go"
      provides: "Response shape tests for layer status, trace, query"
      min_lines: 50
    - path: "internal/ingest/handler_apps_test.go"
      provides: "Apps CRUD handler tests"
      min_lines: 30
  key_links:
    - from: "internal/ingest/handler_response_test.go"
      to: "internal/ingest/receiver_query.go"
      via: "httptest against handler methods"
      pattern: "httptest.NewRecorder"
---

<objective>
Write integration tests for all fixed and new endpoints using httptest.

Purpose: Verify response shapes match frontend expectations and prevent regression.
Output: Two test files covering response shapes and apps CRUD.
</objective>

<execution_context>
@C:/Users/Drupad/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Drupad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-api-completeness/03-02-SUMMARY.md
@.planning/phases/03-api-completeness/03-03-SUMMARY.md
@internal/ingest/receiver_query.go
@internal/ingest/handler_apps.go
@web/src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Response shape tests for layer status, trace, and query endpoints</name>
  <files>internal/ingest/handler_response_test.go</files>
  <read_first>
    - internal/ingest/receiver_query.go (LayerStatus, TraceResponse, QueryResultResponse structs)
    - internal/ingest/handler_rules.go (ServeListRules)
    - web/src/types/index.ts (expected field names)
  </read_first>
  <action>
Create `internal/ingest/handler_response_test.go` with these tests:

1. **TestLayerStatusResponseShape** — Create a QueryHandler with nil ClickHouse (degraded mode). Call handleGetLayerStatus via httptest. Parse response JSON. Assert:
   - Response has `layers` array with 10 entries
   - First entry has `"layer": "L1_HARDWARE"` (string, not int)
   - First entry has `"status": "gray"` (not "unknown" or "idle")
   - First entry has `"signal_count_5min"` key (not `"signal_count"`)
   - First entry has `"last_signal_time"` key (not `"last_seen_at"`)
   - No `"name"` key present

2. **TestTraceResponseShape** — This is harder without ClickHouse. Test the TraceResponse struct serialization directly:
   - Create a TraceResponse with sample SpanView data
   - Marshal to JSON
   - Assert JSON contains `"spans"` key (not `"signals"`)
   - Assert JSON contains `"detections"` key
   - Assert JSON contains `"duration_ms"` key
   - Assert SpanView has `"start_time"`, `"parent_signal_id"`, `"status"`, `"message"` keys

3. **TestQueryResponseShape** — Test QueryResultResponse struct serialization:
   - Create a QueryResultResponse with sample row objects
   - Marshal to JSON
   - Assert JSON contains `"execution_time_ms"` key
   - Assert `"rows"` value is array of objects (not array of arrays)
   - Assert JSON contains `"total"` key
   - Assert no `"columns"` or `"row_count"` keys

4. **TestRulesNotStub** — Create QueryHandler with a RuleStore containing one test rule. Call handleListRules via httptest. Assert:
   - Status code is 200 (not 501)
   - Response contains `"rules"` array with 1 entry

Use `testing` + `testify/assert`. Use `httptest.NewRecorder` + `httptest.NewRequest`.
  </action>
  <verify>
    <automated>cd C:/Users/Drupad/ArgusXDR && go test ./internal/ingest/... -run "TestLayerStatusResponseShape|TestTraceResponseShape|TestQueryResponseShape|TestRulesNotStub" -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/ingest/... -run TestLayerStatusResponseShape -count=1` exits 0
    - `go test ./internal/ingest/... -run TestTraceResponseShape -count=1` exits 0
    - `go test ./internal/ingest/... -run TestQueryResponseShape -count=1` exits 0
    - `go test ./internal/ingest/... -run TestRulesNotStub -count=1` exits 0
  </acceptance_criteria>
  <done>All four response shape tests pass, verifying frontend-compatible JSON field names.</done>
</task>

<task type="auto">
  <name>Task 2: Apps handler unit tests</name>
  <files>internal/ingest/handler_apps_test.go</files>
  <read_first>
    - internal/ingest/handler_apps.go (handler implementations)
    - internal/ingest/receiver_query.go (QueryHandler struct, NewQueryHandler)
  </read_first>
  <action>
Create `internal/ingest/handler_apps_test.go` with tests:

1. **TestListAppsNoPool** — Create QueryHandler with nil pool. Call handleListApps. Assert status 503 and error message "storage unavailable".

2. **TestCreateAppResponseShape** — This needs a real PostgreSQL connection (integration test). Use a build tag `//go:build integration` at the top. If pool is available:
   - POST to handleCreateApp with `{"name": "test-app", "description": "test"}`
   - Assert status 201
   - Assert response has `"api_key"` field starting with `"arg_"`
   - Assert response has `"api_key_prefix"` field
   - Assert response has `"id"` field

3. **TestCreateAppValidation** — Create QueryHandler with nil pool. Call handleCreateApp with empty name. Assert status 400 (validation runs before pool check... actually pool check runs first). Adjust: with nil pool, assert 503. With non-nil pool but empty name, assert 400. For this unit test without DB, just test the nil-pool case.

Keep tests simple. The response shape tests in Task 1 are the critical gate. These are defense-in-depth.
  </action>
  <verify>
    <automated>cd C:/Users/Drupad/ArgusXDR && go test ./internal/ingest/... -run "TestListAppsNoPool" -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/ingest/... -run TestListAppsNoPool -count=1` exits 0
    - File `internal/ingest/handler_apps_test.go` exists
  </acceptance_criteria>
  <done>Apps handler tests verify graceful degradation when PostgreSQL unavailable.</done>
</task>

</tasks>

<verification>
- `go test ./internal/ingest/... -count=1` passes (all tests including new ones)
- Response shape tests confirm frontend-compatible JSON
</verification>

<success_criteria>
- All response shape tests pass
- Apps degradation test passes
- No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/03-api-completeness/03-05-SUMMARY.md`
</output>
