---
phase: quick
plan: 260416-m9t
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/resilience/rate_limiter.go
  - tests/integration/pipeline_integration_test.go
  - tests/unit/baseline/engine_test.go
  - cmd/benchmark/load_test.go
  - tests/unit/signal/signal_layers_test.go
autonomous: true
requirements: [BUILD-STABLE, SIGNAL-VALIDATION]

must_haves:
  truths:
    - "`go build ./...` produces zero errors (excluding gen/go/google which is deleted)"
    - "`go test -c ./tests/integration/` compiles without error"
    - "`go test -c ./tests/unit/baseline/` compiles without error"
    - "`go test -c ./cmd/benchmark/` compiles without error"
    - "`go test ./tests/unit/signal/` passes — all 11 context layers round-trip correctly"
  artifacts:
    - path: "tests/unit/signal/signal_layers_test.go"
      provides: "Proto round-trip tests for all 11 context layers"
      min_lines: 150
  key_links:
    - from: "tests/unit/signal/signal_layers_test.go"
      to: "gen/go/argus/v1/signal.pb.go"
      via: "proto Marshal/Unmarshal"
      pattern: "proto\\.(Marshal|Unmarshal)"
---

<objective>
Fix all remaining Go build failures and add signal validation tests for all 11 context layers (L1-L10 + LDecision).

Purpose: Unblock all downstream development by making the entire codebase compile cleanly, and validate that the proto schema rewrite (260416-lxk) produces correct round-trip serialization for every layer.

Output: Zero build errors, passing signal layer tests.
</objective>

<execution_context>
@C:/Users/Drupad/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Drupad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/AUDIT.md
@gen/go/argus/v1/signal.pb.go
@proto/argus/v1/signal.proto
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all Go build failures</name>
  <files>internal/resilience/rate_limiter.go, tests/integration/pipeline_integration_test.go, tests/unit/baseline/engine_test.go, cmd/benchmark/load_test.go</files>
  <read_first>
    - internal/resilience/rate_limiter.go (missing "context" import)
    - tests/integration/pipeline_integration_test.go (proto field mismatches)
    - tests/unit/baseline/engine_test.go (field name mismatches)
    - cmd/benchmark/load_test.go (old Layer enum names)
    - gen/go/argus/v1/signal.pb.go lines 1-100 (current enum/field names)
  </read_first>
  <action>
**1. Delete `gen/go/google/` directory entirely.**
No ArgusXDR code imports these paths (confirmed via grep). They cause multi-package-per-directory build errors.
```bash
rm -rf gen/go/google/
```

**2. Fix `internal/resilience/rate_limiter.go`:**
Add `"context"` to the import block. Lines 64 and 80 reference `context.Context` but the import is missing.

**3. Fix `tests/integration/pipeline_integration_test.go`:**
Current errors and fixes:
- `v1.Layer_L5_SERVICE` -> `v1.Layer_L5_OUTPUT_DECODING` (Layer enum renamed)
- `pipeline.NewSchemaValidator()` -> `pipeline.NewSchemaValidator(logger)` and capture both return values `(validator, err)`
- `EventData` / `ArgusSignal_Event` / `v1.Event` -> use the correct `oneof context` field, e.g. `ContextL5` with `GetContextL5()`
- `chain.Start(ctx)` returns no value -> remove assignment, just call `chain.Start(ctx)`
Read the file first to understand the full test intent, then fix each error surgically.

**4. Fix `tests/unit/baseline/engine_test.go`:**
- `Count` -> `SampleCount` (field renamed in BaselineProfile proto)
- `Stddev` -> `StdDev` (case mismatch)
- `profile.Count` -> `profile.SampleCount` (or the internal baseline type equivalent)
- `fmt.IsNaN` / `fmt.IsInf` -> `math.IsNaN` / `math.IsInf` (wrong package, needs `"math"` import)

**5. Fix `cmd/benchmark/load_test.go`:**
Old enum names like `pb.Layer_LAYER_HARDWARE`, `pb.Layer_LAYER_OS`, etc. do not exist.
Map to current enums:
- `Layer_LAYER_HARDWARE` -> `Layer_L1_HARDWARE`
- `Layer_LAYER_OS` -> `Layer_L2_MODEL_WEIGHTS` (closest mapping)
- `Layer_LAYER_CONTAINER` -> `Layer_L3_TOKENIZER`
- `Layer_LAYER_RUNTIME` -> `Layer_L4_TRANSFORMER`
- `Layer_LAYER_LIBRARY` -> `Layer_L5_OUTPUT_DECODING`
- `Layer_LAYER_FRAMEWORK` -> `Layer_L6_SAFETY`
- `Layer_LAYER_APPLICATION` -> `Layer_L7_RAG_RETRIEVAL`
- `Layer_LAYER_NETWORK` -> `Layer_L8_AGENTS`
- `Layer_LAYER_DATA` -> `Layer_L9_API_GATEWAY`
- `Layer_LAYER_INTEGRATION` -> `Layer_L10_APPLICATION`
The benchmark just needs valid Layer enum values; exact semantic mapping doesn't matter for load testing.
  </action>
  <verify>
    <automated>go build ./... 2>&1 | grep -v "^$" | wc -l</automated>
  </verify>
  <done>
    - `go build ./...` exits 0 with zero error output
    - `go test -c ./tests/integration/` exits 0
    - `go test -c ./tests/unit/baseline/` exits 0
    - `go test -c ./cmd/benchmark/` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Signal layer round-trip validation tests</name>
  <files>tests/unit/signal/signal_layers_test.go</files>
  <read_first>
    - gen/go/argus/v1/signal.pb.go lines 150-500 (ArgusSignal struct, context oneof)
    - proto/argus/v1/signal.proto (all ContextL* message definitions)
  </read_first>
  <action>
Create `tests/unit/signal/signal_layers_test.go` with package `signal_test`.

For each of the 11 layers (L1-L10 + LDecision), write one subtest in `TestSignalLayerRoundTrip`:

```go
func TestSignalLayerRoundTrip(t *testing.T) {
    // For each layer:
    // 1. Create ArgusSignal with signal_id, trace_id, layer enum, and the context oneof set
    // 2. Set 2-3 representative fields in the context struct
    // 3. proto.Marshal the signal
    // 4. proto.Unmarshal into a new ArgusSignal
    // 5. Assert the context fields survived round-trip
}
```

Specific subtests:
- **L1_HARDWARE**: Set `CpuUsagePct=85.5`, `GpuCount=4`, verify both survive
- **L2_MODEL_WEIGHTS**: Set `ModelId="llama-3"`, `ParameterCount=70_000_000_000`, verify
- **L3_TOKENIZER**: Set `InputTokens=1024`, `TokenizerId="cl100k_base"`, verify
- **L4_TRANSFORMER**: Set `KvCacheUsedMb=2048.0`, `FlashAttention=true`, verify
- **L5_OUTPUT_DECODING**: Set `OutputTokens=512`, `Temperature=0.7`, `FinishReason="stop"`, verify
- **L6_SAFETY**: Set `SafetyScore=floatPtr(0.95)`, `Safe=boolPtr(true)`, `JailbreakDetected=boolPtr(false)`, verify
- **L7_RAG_RETRIEVAL**: Set `Operation=VECTOR_SEARCH`, `ResultsCount=10`, `EmbeddingModel="ada-002"`, verify
- **L8_AGENTS**: Set `Operation=TOOL_CALL`, `ToolName="web_search"`, `ToolLatencyMs=150.0`, verify
- **L9_API_GATEWAY**: Set `Method=strPtr("POST")`, `StatusCode=int32Ptr(200)`, `LatencyMs=floatPtr(45.0)`, verify
- **L10_APPLICATION**: Set `EventType=strPtr("chat_submit")`, `Platform=strPtr("web")`, verify
- **L_DECISION**: Set `Decision=ALLOW`, `Confidence=0.99`, `PolicyName="default"`, verify

Use `proto.Marshal` / `proto.Unmarshal` from `google.golang.org/protobuf/proto`.
Use `require.NoError` and `assert.Equal` / `assert.InDelta` from testify.
Use helper functions `floatPtr`, `strPtr`, `boolPtr`, `int32Ptr` for optional fields.
  </action>
  <verify>
    <automated>go test ./tests/unit/signal/ -v -count=1</automated>
  </verify>
  <done>
    - All 11 subtests pass
    - Each subtest confirms proto round-trip preserves layer-specific context fields
    - No external dependencies (pure unit test using only generated proto types)
  </done>
</task>

</tasks>

<verification>
```bash
# Full build clean
go build ./... 2>&1 | wc -l
# Should output: 0

# All signal layer tests pass
go test ./tests/unit/signal/ -v -count=1

# Existing passing tests still pass
go test ./internal/loadtest/ ./internal/notify/... -count=1
```
</verification>

<success_criteria>
- `go build ./...` exits 0 with zero errors
- `go test ./tests/unit/signal/` passes all 11 layer round-trip tests
- No regressions in previously passing packages (loadtest, notify)
</success_criteria>

<output>
After completion, create `.planning/quick/260416-m9t-build-stabilization-and-signal-validatio/260416-m9t-SUMMARY.md`
</output>
